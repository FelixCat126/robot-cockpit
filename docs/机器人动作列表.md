# 宇树G1机器人动作列表

## 📋 概述

宇树G1机器人共有 **29个可动关节**（revolute类型），可以执行丰富的动作。

## 🦵 腿部关节（12个）

### 左腿（6个关节）
- `left_hip_pitch_joint` - 左髋俯仰（前后摆动）
- `left_hip_roll_joint` - 左髋侧摆（内外摆动）
- `left_hip_yaw_joint` - 左髋旋转（左右旋转）
- `left_knee_joint` - 左膝关节（弯曲/伸直）
- `left_ankle_pitch_joint` - 左踝俯仰（前后摆动）
- `left_ankle_roll_joint` - 左踝侧摆（内外摆动）

### 右腿（6个关节）
- `right_hip_pitch_joint` - 右髋俯仰
- `right_hip_roll_joint` - 右髋侧摆
- `right_hip_yaw_joint` - 右髋旋转
- `right_knee_joint` - 右膝关节
- `right_ankle_pitch_joint` - 右踝俯仰
- `right_ankle_roll_joint` - 右踝侧摆

## 🎯 腰部关节（3个）

- `waist_yaw_joint` - 腰部旋转（左右转）
- `waist_roll_joint` - 腰部侧摆（左右倾斜）
- `waist_pitch_joint` - 腰部俯仰（前后弯腰）

## 🤲 左臂关节（6个）

- `left_shoulder_pitch_joint` - 左肩俯仰（前后摆动）
- `left_shoulder_roll_joint` - 左肩侧摆（内外摆动）
- `left_shoulder_yaw_joint` - 左肩旋转（左右旋转）
- `left_elbow_joint` - 左肘关节（弯曲/伸直）
- `left_wrist_roll_joint` - 左腕旋转
- `left_wrist_pitch_joint` - 左腕俯仰
- `left_wrist_yaw_joint` - 左腕偏摆

## 🤲 右臂关节（6个）

- `right_shoulder_pitch_joint` - 右肩俯仰
- `right_shoulder_roll_joint` - 右肩侧摆
- `right_shoulder_yaw_joint` - 右肩旋转
- `right_elbow_joint` - 右肘关节
- `right_wrist_roll_joint` - 右腕旋转
- `right_wrist_pitch_joint` - 右腕俯仰
- `right_wrist_yaw_joint` - 右腕偏摆

## 🎮 当前已实现的动作

### 基础动作
1. **Wave（挥手）** - 右臂抬起并摆动
   - 使用关节：`right_shoulder_pitch_joint`, `right_shoulder_roll_joint`, `right_elbow_joint`
   - 按钮：A（按钮0）

2. **ThumbsUp（点赞）** - 左臂抬起
   - 使用关节：`left_shoulder_pitch_joint`, `left_shoulder_roll_joint`, `left_elbow_joint`
   - 按钮：B（按钮1）

3. **WalkJump（跨栏）** - 抬起右腿
   - 使用关节：`right_hip_pitch_joint`, `right_knee_joint`
   - 按钮：C（按钮2）

4. **Jump（跳跃）** - 抬起左腿
   - 使用关节：`left_hip_pitch_joint`, `left_knee_joint`
   - 按钮：D（按钮3）

5. **RaiseArms（双臂举起）** - 同时抬起双臂
   - 使用关节：`left_shoulder_pitch_joint`, `right_shoulder_pitch_joint`, `left_elbow_joint`, `right_elbow_joint`
   - 按钮：按钮6（新增）

### 移动控制
- **left（左转）** - 机器人整体旋转45度
  - 按钮：LB（按钮4）
- **right（右转）** - 机器人整体旋转45度
  - 按钮：RB（按钮5）

## 💡 可以实现的更多动作建议

### 高级动作（需要组合多个关节）

1. **鞠躬（Bow）**
   - 使用：`waist_pitch_joint` + 轻微弯曲膝盖
   - 已实现，可绑定到按钮

2. **下蹲（Squat）**
   - 使用：`left_hip_pitch_joint`, `right_hip_pitch_joint`, `left_knee_joint`, `right_knee_joint`
   - 已实现，可绑定到按钮

3. **转头（TurnHead）**
   - 使用：`waist_yaw_joint`（模拟转头）
   - 已实现，可绑定到按钮

4. **双臂交叉**
   - 使用：`left_shoulder_roll_joint`, `right_shoulder_roll_joint`, `left_elbow_joint`, `right_elbow_joint`

5. **单腿站立**
   - 使用：`left_hip_pitch_joint`, `left_knee_joint`, `right_hip_pitch_joint`, `right_knee_joint`, `right_ankle_pitch_joint`

6. **侧踢**
   - 使用：`right_hip_roll_joint`, `right_hip_pitch_joint`, `right_knee_joint`

7. **前踢**
   - 使用：`right_hip_pitch_joint`, `right_knee_joint`, `right_ankle_pitch_joint`

8. **挥手告别**
   - 使用：`right_shoulder_pitch_joint`, `right_shoulder_yaw_joint`, `right_elbow_joint`, `right_wrist_pitch_joint`

9. **敬礼**
   - 使用：`right_shoulder_pitch_joint`, `right_elbow_joint`, `right_wrist_pitch_joint`

10. **瑜伽姿势**
    - 使用：多个关节组合，如`waist_pitch_joint` + `left_hip_pitch_joint` + `left_knee_joint`

## 🎯 关节角度范围

根据URDF文件中的limit定义，各关节都有运动范围限制。例如：
- `left_hip_pitch_joint`: -2.53 到 2.88 弧度（约-145°到165°）
- `left_knee_joint`: 通常 -2.0 到 0 弧度（约-115°到0°）

## 📝 使用说明

所有动作都支持"按下"和"松开"事件：
- **按下按钮**：执行动作
- **松开按钮**：恢复默认姿态（动作名 + "_release"）

可以通过修改 `Robot3DViewer.tsx` 中的 `useEffect` 来添加新动作，或修改 `PeripheralController.tsx` 来绑定按钮。
