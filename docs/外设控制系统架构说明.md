# 外设控制系统架构说明 (Peripheral System Architecture)
**📝 文档说明：** 外设控制系统的技术架构文档，面向开发者的详细设计说明

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户界面层                             │
│  ┌────────────────┐        ┌──────────────────────────┐    │
│  │ ControlPanel   │        │ PeripheralDebugPanel     │    │
│  │ (控制面板)      │        │ (调试面板)               │    │
│  └────────┬───────┘        └───────────┬──────────────┘    │
└───────────┼─────────────────────────────┼──────────────────┘
            │                             │
            v                             v
┌─────────────────────────────────────────────────────────────┐
│                       React组件层                            │
│  ┌────────────────────────────────────────────────────┐    │
│  │         PeripheralController                       │    │
│  │         (外设控制器组件)                            │    │
│  └─────────────┬──────────────────────────────────────┘    │
└────────────────┼─────────────────────────────────────────────┘
                 │
                 v
┌─────────────────────────────────────────────────────────────┐
│                       核心逻辑层                             │
│  ┌───────────────────┐         ┌────────────────────┐      │
│  │ PeripheralManager │◄────────┤  InputMapper       │      │
│  │ (外设管理器)       │         │  (输入映射器)       │      │
│  └────────┬──────────┘         └────────────────────┘      │
│           │                                                 │
│           ├──► GamepadDevice  (游戏手柄)                   │
│           ├──► KeyboardDevice (键盘)                       │
│           └──► CustomDevice   (自定义外设，预留)            │
└─────────────────────────────────────────────────────────────┘
                 │
                 v
┌─────────────────────────────────────────────────────────────┐
│                     浏览器API层                              │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Gamepad API │  │ Keyboard API │  │ WebHID (预留) │      │
│  └─────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                 │
                 v
┌─────────────────────────────────────────────────────────────┐
│                     通信服务层                               │
│  ┌───────────────────────────────────────────────────┐     │
│  │  communicationService (WebSocket / WebRTC)        │     │
│  └───────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
                 │
                 v
┌─────────────────────────────────────────────────────────────┐
│                      ROS系统                                 │
│  /cmd_vel, /robot/commands, /emergency_stop, etc.          │
└─────────────────────────────────────────────────────────────┘
```

## 📦 核心模块

### 1. 类型定义 (`peripheral.types.ts`)
- `PeripheralType`: 外设类型枚举
- `InputEvent`: 统一的输入事件结构
- `RobotCommand`: 机器人命令结构
- `IPeripheralDevice`: 设备接口

### 2. 设备层
#### BasePeripheralDevice (抽象基类)
- 提供事件管理
- 死区处理
- 状态管理

#### GamepadDevice (游戏手柄适配器)
- 连接Gamepad API
- 60Hz轮询
- 按钮边缘检测
- 轴向变化检测
- 自动重连机制（最多10次，指数退避）

#### KeyboardDevice (键盘适配器)
- 键盘事件监听
- 按键状态管理
- 备用控制方案

### 3. 管理层
#### PeripheralManager (外设管理器)
- 统一管理多个设备
- 设备生命周期控制
- 事件转发和聚合
- 健康检查（10s 间隔）

#### InputMapper (输入映射器)
- 规则引擎
- 输入→命令转换
- 动态规则管理

### 4. React组件层
#### PeripheralController
- 集成管理器和映射器
- 处理命令发送
- 生命周期管理
- 命令节流（10Hz）和防抖（300ms）

#### PeripheralDebugPanel
- 实时状态显示
- 事件日志
- 设备信息展示
- 重连提示

## 🔄 数据流

### 输入捕获流程
```
1. 物理外设输入
   ↓
2. 浏览器API捕获 (Gamepad/Keyboard)
   ↓
3. Device类处理 (GamepadDevice/KeyboardDevice)
   - 应用死区
   - 边缘检测
   - 生成InputEvent
   ↓
4. PeripheralManager转发
   ↓
5. PeripheralController处理
   - 摇杆：合并轴状态，计算速度和动画
   - 按钮：映射到具体动作
   ↓
6. 生成RobotCommand
   ↓
7. 通过WebSocket/WebRTC发送
   ↓
8. 更新3D机器人状态 (robot3DStore)
   ↓
9. Robot3DViewer响应并播放动画
```

## 📂 文件结构

```
frontend/src/
├── types/
│   └── peripheral.types.ts          # 类型定义
├── utils/
│   └── peripherals/
│       ├── BasePeripheralDevice.ts  # 设备基类
│       ├── GamepadDevice.ts         # 游戏手柄适配器
│       ├── KeyboardDevice.ts        # 键盘适配器
│       ├── PeripheralManager.ts     # 外设管理器
│       ├── InputMapper.ts           # 输入映射器
│       └── index.ts                 # 统一导出
├── components/
│   └── shared/
│       ├── PeripheralController.tsx # 控制器组件
│       ├── PeripheralDebugPanel.tsx # 调试面板组件
│       ├── PeripheralDebugPanel.css # 样式文件
│       └── ControlPanel.tsx         # 主控制面板（已集成）
├── stores/
│   └── robot3DStore.ts              # 3D机器人状态管理
└── screens/
    └── Screen0.tsx                   # 控制屏（已启用外设）

docs/
├── 外设控制系统架构说明.md          # 本文件
├── 外设控制系统使用指南.md          # 用户使用指南
├── 外设控制快速开始.md              # 快速上手指南
└── 外设控制故障排查.md              # 问题排查指南
```

## 🎯 设计原则

### 1. **可扩展性**
- 接口驱动设计 (`IPeripheralDevice`)
- 插件化架构
- 易于添加新设备类型

### 2. **解耦合**
- 设备层独立于UI
- 映射规则与设备无关
- 通信协议可切换

### 3. **高性能**
- 60Hz采样率
- 事件驱动
- 避免不必要的重渲染
- 命令节流和防抖

### 4. **易用性**
- 零配置即用（Gamepad）
- 合理的默认映射
- 实时调试工具

### 5. **健壮性**
- 自动重连机制
- 防抖和节流
- 错误处理和恢复

## 🔧 配置和定制

### 添加新设备类型

1. **实现设备类**
```typescript
export class MyDevice extends BasePeripheralDevice {
  constructor() {
    super('my-device', PeripheralType.CUSTOM, 'My Device');
  }

  async connect() {
    // 连接逻辑
  }

  async disconnect() {
    // 断开逻辑
  }

  getState(): PeripheralState {
    // 返回状态
  }
}
```

2. **注册到管理器**
```typescript
const manager = new PeripheralManager();
const myDevice = new MyDevice();
manager.addDevice(myDevice);
```

### 修改默认映射

编辑 `InputMapper.ts` 中的 `createDefaultInputMapping()` 函数。

### 调整采样率

```typescript
const gamepad = new GamepadDevice(0, {
  sampleRate: 30, // 降低到30Hz
  deadzone: 0.2,  // 增大死区
});
```

## 🧪 测试

### 手动测试清单
- [ ] Gamepad连接/断开
- [ ] 摇杆输入（死区、范围）
- [ ] 按钮按下/松开
- [ ] 键盘输入
- [ ] 命令发送
- [ ] 调试面板显示
- [ ] 多设备同时使用
- [ ] 自动重连机制

### 浏览器兼容性
| 浏览器 | Gamepad API | WebHID | Web Serial |
|--------|-------------|--------|------------|
| Chrome | ✅ | ✅ | ✅ |
| Edge   | ✅ | ✅ | ✅ |
| Firefox| ✅ | ❌ | ❌ |
| Safari | ⚠️ | ❌ | ❌ |

## 🚀 性能优化

### 1. 降低发送频率
```typescript
// PeripheralController.tsx
const sendIntervalMs = 100; // 10Hz，可调整为 200 (5Hz)
```

### 2. 节流处理
```typescript
let lastSendTime = 0;
const throttleMs = 33; // 30Hz

if (now - lastSendTime < throttleMs) {
  return;
}
```

### 3. 批量发送
- 合并多个轴的数据为一个命令
- 减少网络请求

### 4. 使用WebRTC
- 生产环境推荐WebRTC Data Channel
- 延迟更低（<100ms）

## 🐛 已知限制

1. **Safari支持有限**
   - Gamepad API支持不完整
   - 建议使用Chrome/Edge

2. **蓝牙延迟**
   - 蓝牙连接可能有额外50-100ms延迟
   - 建议使用有线连接

3. **刷新页面**
   - 需要重新连接设备
   - 浏览器安全限制

4. **多窗口问题**
   - 同一设备只能被一个窗口使用
   - 需要协调多屏场景

## 📈 未来计划

- [ ] 配置UI界面（可视化编辑映射规则）
- [ ] 配置导入/导出（JSON格式）
- [ ] 录制和回放功能
- [ ] 力反馈支持
- [ ] 多手柄支持
- [ ] WebHID设备支持
- [ ] 性能监控面板

## 📚 相关资源

- [Gamepad API 文档](https://developer.mozilla.org/en-US/docs/Web/API/Gamepad_API)
- [WebHID API 文档](https://developer.mozilla.org/en-US/docs/Web/API/WebHID_API)
- [Web Serial API 文档](https://developer.mozilla.org/en-US/docs/Web/API/Web_Serial_API)

## 📞 相关文档

- [外设控制快速开始](./外设控制快速开始.md) - 快速上手指南
- [外设控制系统使用指南](./外设控制系统使用指南.md) - 完整使用手册
- [外设控制故障排查](./外设控制故障排查.md) - 问题排查指南
- [3D机器人可视化说明](./3D机器人可视化说明.md) - 3D可视化功能

---

**创建日期：** 2024年12月8日  
**作者：** Robot Cockpit Team  
**版本：** 1.0.0

